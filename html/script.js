function toggleNode() {
    const html = document.documentElement

    //if (html.classList.contains('light')) {
        //html.classList.remove('light')
    //} else { 
        //html.classList.add
    //}

    html.classList.toggle('light')


    //substituir a imagem aproveitando a lógica da mudança para o light
   /*const img = document.querySelector("#profile img") 
        if (html.classList.contains('light')) {
            img.setAttribute("src", "/caminhdodaimagem.png")

        } else {
            img.setAttribute("src", "caminhodaimagempadrao.png")
        }

   }
   
   */


}